<%= form_with(model: student_audio, local: true) do |form| %>
  <% if student_audio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_audio.errors.count, "error") %> prohibited this student_audio from being saved:</h2>

      <ul>
        <% student_audio.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :audio_student %>
    <%= form.text_area :audio_student %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :task_id %>
    <%= form.text_field :task_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
